import type { NextPage } from 'next';

import TictactoeMultiplayerMatches from 'src/containers/tictactoe-multiplayer-matches/TictactoeMultiplayerMatches';

const TictactoeMultiplayerMatchesPage: NextPage = () => <TictactoeMultiplayerMatches />;

export default TictactoeMultiplayerMatchesPage;
