import type { NextPage } from 'next';

import TictactoeMultiplayerMatch from 'src/containers/tictactoe-multiplayer-match/TictactoeMultiplayerMatch';

const TictactoeMultiplayerMatchPage: NextPage = () => <TictactoeMultiplayerMatch />;

export default TictactoeMultiplayerMatchPage;
